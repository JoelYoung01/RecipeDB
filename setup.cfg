[flake8]
max-line-length = 88
extend-ignore = E203
exclude = .git,__pycache__,build,dist,alembic/versions
per-file-ignores =
    # Long recipe seed strings are intentional content, not formatting debt.
    api/core/seed_database.py: E501
